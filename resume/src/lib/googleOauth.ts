// Shared constants for the Google OAuth flow (route files may only export HTTP handlers,
// so these live here instead of in a route.ts).
export const GOOGLE_OAUTH_STATE_COOKIE = "dresume_goauth_state";
