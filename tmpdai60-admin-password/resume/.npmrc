# Use hoisted node_modules layout — avoids pnpm's symlink strategy that
# breaks when switching between Git Bash (Unix symlinks) and PowerShell
# (Windows junctions). All packages get a flat directory like npm/yarn.
node-linker=hoisted
